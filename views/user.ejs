<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>hii i am user page</h1>
    <p>Hello, <%= user.role %>!</p>
       <% if (user.role=='admin') { %>
          <h2>Welcome, admin </h2>  
       <% }else{ %>
            <% if (user.role=='distributor') { %>
                <h2>Welcome, distributor </h2>
                <button><a href="/product/addProduct">Add Product</a></button>
            <% } %>
       <% } %>

</body>
</html>